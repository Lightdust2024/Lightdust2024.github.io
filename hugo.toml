baseURL = 'https://Lightdust2024.github.io/'
locale = 'zh'
title = 'Lightdust的个人博客'
theme = "stack"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[frontmatter]
  lastmod = [":git", ":fileModTime"]

#允许获取Git信息
enableGitInfo = true